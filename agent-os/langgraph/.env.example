# Never commit real secrets.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Local orchestration
LANGGRAPH_URL=http://127.0.0.1:2024
LANGSMITH_API_KEY=
ODOO_URL=http://127.0.0.1:8069

# Optional persistent LangGraph checkpoint store
# Example only: postgresql://user:password@localhost:5432/aspar?sslmode=disable
LANGGRAPH_POSTGRES_URI=

# Worker identity — credential must be provisioned outside Git
ASPAR_WORKER_ID=
ASPAR_WORKER_TOKEN=

# Second LangGraph-orchestrated agent (second_agent_worker.py) — Gemini via
# Google AI Studio's free tier (aistudio.google.com/apikey). Sign in with an
# existing Google account — no business verification, no credit card.
# NVIDIA Build was tried first and abandoned 2026-09-14 (account
# verification blocked with no timeline). Never reuse a key pasted in chat.
GEMINI_API_KEY=
