# Never commit real secrets.

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Local orchestration
LANGGRAPH_URL=http://127.0.0.1:2024
LANGSMITH_API_KEY=
ODOO_URL=http://127.0.0.1:8069

# Optional persistent LangGraph checkpoint store
# Example only: postgresql://user:password@localhost:5432/aspar?sslmode=disable
LANGGRAPH_POSTGRES_URI=

# Worker identity — credential must be provisioned outside Git
ASPAR_WORKER_ID=
ASPAR_WORKER_TOKEN=

# Second LangGraph-orchestrated agent (second_agent_worker.py) — DeepSeek's
# own official API, free tier (platform.deepseek.com/api_keys: 5M tokens,
# 30 days, no credit card). NVIDIA Build and Gemini were tried first and
# reverted 2026-09-14 (NVIDIA: account verification blocked with no
# timeline; Gemini: not DeepSeek). Must be a NEW key — the one pasted into
# chat on 2026-09-14 is permanently banned regardless of rotation.
DEEPSEEK_API_KEY=
